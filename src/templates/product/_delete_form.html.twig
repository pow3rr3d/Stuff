<input type="hidden" name="_token" value="{{ csrf_token('update' ~ product.id) }}">
<button class="btn btn-danger" data-toggle="modal"
        data-target="#modal_delete{{ product.id }}">Delete
</button>