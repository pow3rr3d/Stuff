{# <form method="post" action="{{ path('myloanedstuffs_delete', {'id': loan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loan.id) }}">
    <button class="btn">Delete</button>
</form> #}
<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ loan.id) }}">
<button class="btn btn-danger" data-toggle="modal"
        data-target="#modal_delete{{ loan.id }}">Delete
</button>
