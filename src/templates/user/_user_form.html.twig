<input type="hidden" name="_token" value="{{ csrf_token('update' ~ user.id) }}">
<button class="btn btn-danger" data-toggle="modal"
        data-target="#modal_user{{ user.id }}">Make User
</button>